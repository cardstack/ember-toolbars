{{demo-nav}}

{{sample-buttons model=model }}

{{lorem-ipsum}}

{{sample-buttons model=model }}

This is the last bit of text.

{{#if model.left}}
  {{in-left-toolbar show=(component model.left)}}
{{/if}}

{{#if model.right}}
  {{in-right-toolbar show=(component model.right) }}
{{/if}}

{{#if model.top}}
  {{in-top-toolbar show=(component model.top) }}
{{/if}}

{{#if model.bottom}}
  {{in-bottom-toolbar show=(component model.bottom) }}
{{/if}}
