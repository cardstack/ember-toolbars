{{sample-buttons showLeft=(action (mut showLeft))
                 showRight=(action (mut showRight))
                 showTop=(action (mut showTop))
                 showBottom=(action (mut showBottom)) }}

{{lorem-ipsum}}

{{sample-buttons showLeft=(action (mut showLeft))
                 showRight=(action (mut showRight))
                 showTop=(action (mut showTop))
                 showBottom=(action (mut showBottom)) }}

This is the last bit of text.

{{#if showLeft}}
  {{to-elsewhere named="cst-left" send=(component showLeft) }}
{{/if}}

{{#if showRight}}
  {{to-elsewhere named="cst-right" send=(component showRight) }}
{{/if}}

{{#if showTop}}
  {{to-elsewhere named="cst-top" send=(component showTop) }}
{{/if}}

{{#if showBottom}}
  {{to-elsewhere named="cst-bottom" send=(component showBottom) }}
{{/if}}
