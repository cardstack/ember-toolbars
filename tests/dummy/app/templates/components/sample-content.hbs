{{sample-buttons showLeft=(action (mut showLeft))
                 showRight=(action (mut showRight))
                 showTop=(action (mut showTop))
                 showBottom=(action (mut showBottom)) }}

{{lorem-ipsum}}

{{sample-buttons showLeft=(action (mut showLeft))
                 showRight=(action (mut showRight))
                 showTop=(action (mut showTop))
                 showBottom=(action (mut showBottom)) }}

This is the last bit of text.

{{#if showLeft}}
  {{in-left-toolbar show=(component showLeft)}}
{{/if}}

{{#if showRight}}
  {{in-right-toolbar show=(component showRight) }}
{{/if}}

{{#if showTop}}
  {{in-top-toolbar show=(component showTop) }}
{{/if}}

{{#if showBottom}}
  {{in-bottom-toolbar show=(component showBottom) }}
{{/if}}
